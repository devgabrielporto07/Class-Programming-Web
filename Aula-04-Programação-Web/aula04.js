// Sintaxe:

console.log ("Olá Mundo!");
const nome = "Web Coding";
console.log ("2" + 3);
let nota = 8.5;

if (nota >= 6 ) {
    console.log (`${nome}: aprovado!`);
}


let oi = "2" // Forma de declarar let para reatribuir
let caju = 2

// Fracamente tipado:

if (oi == caju) { // if ( oi === caju) => é diferente
    console.log("É igual")
}
else {
    console.log("Diferente")
}

// Valores fixos com const:

const PI = 3.14;
const NOME_ESCOLA = "Uninassau";

// PI = 4; Error

// == compara só o valor

// === compara o valor e o tipo

// !== diferente de
