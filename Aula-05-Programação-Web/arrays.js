const notas = [7, 8.5, 6];

console.log (notas [0])
console.log (notas [1])
console.log (notas.length);

notas.push (9);
notas.pop ();
notas.unshift (5);
notas.sort();
notas.reverse();
notas.join ("-")
console.log (notas);
const arrays = [1, 4.5, 3, "Gabriel", "Ney"]

for (let i = 0; i < arrays.length; i++) {
    console.log (arrays[i]);
}

